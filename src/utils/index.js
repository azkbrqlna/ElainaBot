const { handleMenuCommand } = require("../handler/handleMenuCommand");
const { handleStickerCommand } = require("../handler/handleStickerCommand");
const { handleStealCommand } = require("../handler/handleStealCommand");
const { handleHideTagCommand } = require("../handler/handleHideTagCommand");
const { handleToImgCommand } = require("../handler/handleToImgCommand");
const { handleBuffCommand } = require("../handler/handleBuffCommand");

module.exports = {
  handleMenuCommand,
  handleStickerCommand,
  handleStealCommand,
  handleHideTagCommand,
  handleToImgCommand,
  handleBuffCommand,
};
